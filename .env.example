# Environment Configuration
NODE_ENV=production

# Session Secret (REQUIRED - Change this to a random secret key)
# Generate a secure random string for production use
SESSION_SECRET=your-super-secret-key-here-change-this-in-production

# Database Configuration (Required for persistent data storage)
# For Vercel deployment, consider using Neon, Supabase, or PlanetScale
DATABASE_URL=postgresql://username:password@hostname:port/database

# Optional: Custom domain for QR code short links
# If not set, will use the Vercel domain
# CUSTOM_DOMAIN=yourdomain.com
